# Project 1 - Structured Express API

A basic Express.js API demonstrating layered backend architecture,
middleware chains, environment configuration, structured logging,
and correlation IDs.

## Architecture

Routes → Controllers → Services → Repositories

## Endpoints

GET /health
GET /api/users
GET /api/users/:id

## Running locally

npm install
npm run dev
